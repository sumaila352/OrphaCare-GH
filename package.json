{
  "name": "orphacare-gh",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "postinstall": "npm run db:generate -w apps/api",
    "dev": "npx concurrently -n api,web -c blue,green \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "dev:api": "npm run dev -w apps/api",
    "dev:web": "npm run dev -w apps/web",
    "build": "npm run build -w apps/api && npm run build -w apps/web",
    "build:production": "npm run build:production -w apps/api",
    "start": "npx concurrently -n api,web -c blue,green \"npm run start -w apps/api\" \"npm run start -w apps/web\"",
    "db:migrate": "npm run db:migrate -w apps/api",
    "db:seed": "npm run db:seed -w apps/api",
    "db:deploy": "npm run db:deploy -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
